# SPOT 사용성·신뢰성 개선 (2026-09-21)

## 화면 분리
- 대시보드: 오늘 일정, 간결한 루틴 바로가기, 접을 수 있는 첫 사용 안내.
- 일정 / 루틴 / 우리 크루 / 미션 / 음악 탭으로 기능 분리.
- 알림 진단은 알림 창, 이용 통계는 관리자에게만 표시한다.

## 미션 규칙
- 기존 미션 rules_version=1은 소급해서 변경하지 않는다. 새 미션은 rules_version=2다.
- 시작 이후 새 참여는 마감된다. 시작 후 중도포기·진행 중 크루 탈퇴는 미달성으로 남으며 정산 대상에서 제외되지 않는다. 시작 전 탈퇴는 참여 취소다.
- 확인된 출석은 이후 일정 변경으로 없어지지 않는다. 출석·확인·일정 변경·중도포기·이의 검토 이력을 기록한다.
- 참여자는 정산 확정 전 이의를 남길 수 있다. 작성자/크루장이 검토 답변을 남긴다. 미처리 이의가 있으면 커피 정산 확정이 차단된다.
- 확정 시 정산의 진행 결과도 함께 저장해 이후 변화와 분리한다.
- 이 기능은 크루원 증언 기반이며 위치·실제 운동량 인증이 아니다. 현금 보관·자동 결제는 하지 않는다.

## 시작 안내·알림
- 초대 링크 로그인 화면에 확인된 크루 이름 표시.
- 크루 합류 → 일정 참여 → 푸시 테스트 안내.
- 현재 브라우저 알림 권한/구독 상태, 본인 기기로 테스트 푸시 발송, 수신 확인, 복구 안내.
- 테스트는 계정당 1분 간격, 실제 등록된 본인 구독에만 전송한다. 발송 접수와 기기 수신 확인은 별개다.
- 연결 오류일 때 로그아웃으로 단정 표시하지 않으며 초기 로딩 중 기본 루틴을 보여주지 않는다.

## 운동 진행
- 입문 전신 예시 2개를 기존 루틴에 영향 없이 추가 가능.
- 루틴 카드 운동 시작 → 종목별 세트 체크 → 시간 기준 휴식 타이머 → 다음 종목.
- 무게/운동량 기록을 요구하지 않는다. 진행 체크는 현재 화면 안에서만 사용한다.
- 같은 부위의 대체 후보를 가이드와 함께 선택할 수 있다. 원본 루틴은 바꾸지 않는다.
- 새 운동도 라이브러리에서 선택해 추가하면 이름 기반 가이드가 연결된다. 지원하지 않는 임의 이름은 준비 중 안내를 제공한다.

## 친구 응원·통계
- 확인된 출석에 본인 외 크루원이 1회 응원, 주간 출석 날짜 수, 미션 달성 메시지 복사.
- usage_daily는 임의 브라우저 ID·서울 날짜·로그인 사용자 ID(있을 때)·조회 시간만 저장한다. IP/초대 URL은 저장하지 않는다.
- 관리자만 방문 브라우저·오늘 방문·다른 날짜 재방문·누적 로그인·가입·일정 참여·출석 인원을 조회한다.
- 수집 전 방문자는 복구되지 않는다. 브라우저 방문 수는 정확한 사람 수가 아니며, 계정/참여 누적 수와 같은 분모의 전환율이 아니다.
