# 운동 도감 확장

기존 22개 가이드에 154개 운동별 가이드를 추가했다. 부위/기구/한글·영문 검색, 줄임말 검색, 24개 단위 더보기를 제공한다. 같은 이름을 복제해 개수를 늘리지 않는다.

## 출처와 재현
- 선택 목록과 한국어 설명: `data/exercise-library-expansion.json`.
- 공개 wger exerciseinfo 자료의 기존 다운로드 스냅샷을 사용했다. 스냅샷 SHA-256: `1d3b5a56ac4868391766f32753da06ca7025f923315944e334d307b72acb61ba`.
- 생성기 `scripts/expand-exercise-library.py`는 `.data/wger-exercises.json`, `.data/wger-licenses.json`을 입력으로 사용한다. 공개 원본 API: `https://wger.de/api/v2/exerciseinfo/`, `https://wger.de/api/v2/license/`.
- 이번 추가 이미지 205개 연결. 원본 중 이미 보유한 파일은 재사용했다. 이미지별 작가, CC 라이선스, 원본 URL 및 해시를 `lib/exercise-images.json`에 기록한다.
- 원본이 공개한 `is_ai_generated` 표시, 이미지별 저작자 및 라이선스 정보를 확인했다. 저작자가 비어 있거나 AI 생성으로 표시된 이미지는 이번 확장에서 제외했다.
- 이미지 파일은 픽셀을 수정하지 않고 저장했다. 브라우저에는 Next Image가 화면 크기에 맞춰 최적화해 전송하며 원본/라이선스 링크를 제공한다.
- 새 파일은 실제 이미지 서명과 전체 디코딩을 확인했다. 모든 운동의 자세를 전문가가 임상적으로 검증했다는 의미는 아니다.

## 동작 연결과 시간 단위
- 정확한 운동명/별칭 일치를 먼저 사용하고, 그 뒤 기존의 넓은 이름 매칭을 적용한다. 덤벨 벤치, 클로즈 그립 벤치, 바벨 컬, 라잉/시티드 레그 컬을 구분한다.
- 운동 도감의 이름과 영문 별칭을 로컬 루틴 해석기의 운동 사전에도 연결한다.
- 플랭크·정적 스트레칭은 `durationSeconds`로 저장한다. 도감에서 추가할 때 기본 30초를 표시하고 편집할 수 있다. 기존 횟수 운동은 유지한다.
- 루틴 편집/상세/운동 진행에서 초와 횟수를 구분한다. 원문 가져오기에서도 `플랭크 3세트 30초`를 읽고, 휴식 시간을 수행 시간으로 오인하지 않도록 검사한다.
- DB JSON 형식의 선택 필드이므로 별도 스키마 변경은 필요하지 않다.
