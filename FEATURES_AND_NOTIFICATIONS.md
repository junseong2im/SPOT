# 일정·크루·알림 기능

- 반복 일정: 시작일~종료일의 선택 요일에 생성. 최대 180일 범위/60회. 개별 또는 선택 날짜 이후 시리즈 수정·취소.
- 시간 투표: 2~12개 후보, 마감, 복수 선택, 작성자/크루장의 확정. 가능 시간 투표가 참여 신청으로 자동 전환되지는 않음.
- 요일 루틴: 새 일정의 요일에 자동 적용. 기존 일정은 그대로 유지.
- 참여 제한: 정원(1~100명 또는 무제한), 시작 기준 신청 마감. 탈퇴/참여 취소는 신청 마감 이후에도 허용.
- 크루 관리: 크루장만 이름 변경·초대 링크 재발급·크루장 위임 가능. 탈퇴는 soft leave, 마지막 멤버 탈퇴는 보관. 개인 루틴은 물리 삭제하지 않음.
- 알림: 본인 inbox, 모두 읽음, 10분/30분/1시간/하루 전 알림, 일정 변경/취소 알림. 웹 푸시는 사용자 기기에서 명시적으로 허용.

## 저장과 동시 수정

새 상태 필드는 기존 크루 JSON과 호환된다. 스키마 변경은 `supabase/migrations/20260920085314_crew_planning_notifications.sql`에 있다. 한 트랜잭션에서 크루 행을 잠그고 정원 검사·투표 확정·탈퇴·알림 생성을 함께 처리한다. 마지막 자리나 일정 확정의 동시 요청은 중복 처리하지 않는다.

## 웹 푸시 운영

Vercel Production 환경변수: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `CRON_SECRET`, `PUSH_DELIVERY_ENABLED=true`.
VAPID 키는 한 번 생성해 유지한다. 개인키와 CRON_SECRET은 Secret으로 저장한다. 브라우저에는 공개키만 제공한다.

예약 실행은 Supabase `pg_cron` + `pg_net`을 사용한다. `scripts/configure-push-cron.mjs`에 `DATABASE_URL`, `APP_ORIGIN`, `CRON_SECRET`을 환경변수로 제공하면 1분마다 `/api/jobs/notifications`를 호출한다. 호출 키는 Supabase Vault에 저장하고 cron SQL에 평문으로 넣지 않는다. Vercel Hobby의 분 단위 cron에 의존하지 않는다.

실제 발송을 잠시 멈추려면 `PUSH_DELIVERY_ENABLED=false`로 변경한 후 재배포한다. 웹 안 알림은 계속 사용할 수 있다. 이 기능은 카카오/인스타 유료 발송을 사용하지 않지만 Vercel/Supabase 사용량 한도는 적용된다.

## 전달 보장 범위

푸시 서비스의 수락 응답은 실제 기기 표시의 증거가 아니다. OS 절전/알림 차단/네트워크에 따라 지연될 수 있다. iPhone/iPad는 지원되는 OS에서 홈 화면에 추가한 웹 앱을 사용해야 한다. Service Worker는 같은 출처의 일정 화면만 연다.

동시 작업은 알림 행을 원자적으로 선점한다. 중단·부분 실패로 전달 여부가 불명확하면 `unknown`으로 남기고 무조건 재발송하지 않아 중복을 억제한다. 404/410 구독은 비활성화한다. 변경/취소/탈퇴는 이전 예약 알림을 무효화한다.

## 검증

`npm test`의 격리된 PostgreSQL 테스트에 반복 생성, 마지막 자리 경쟁, 확정 중복 방지, 투표 마감, 위임/탈퇴 권한, 시리즈 수정 롤백, 읽음 소유권, 만료 구독, 중복 발송 방지가 포함된다. 푸시 테스트의 전송기는 모의 함수이며 실제 기기 수신을 대신하지 않는다.
