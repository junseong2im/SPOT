# 이미지 용량 최적화

작업 브랜치: `codex/asset-size-optimization`. main 및 운영 배포 변경은 포함하지 않는다.

- 이미지 217개 중 PNG 93개를 무손실 WebP로 변환.
- 전체 디코딩 RGBA와 해상도 동일성을 확인한 파일만 교체. 픽셀 차이가 생긴 46개는 원본 유지. 애니메이션 GIF 유지.
- 이미지 파일 합계: 59,395,028 → 39,884,422 bytes (56.64 → 38.04 MiB), 19,510,606 bytes / 32.8% 절감.
- 원본 출처·작성자·라이선스·기존 해시 보존. 교체 전 로컬 파일은 `.data/asset-originals/<sha256>.png.gz`에 보관. 과거 Git 이력도 그대로 유지.
- 기존 PNG 주소 93개는 명시적 308 리디렉션으로 새 WebP에 연결.
- 전체 Git 과거 이력이나 로컬 캐시를 제거한 작업이 아니므로 `.git` 및 전체 작업 폴더 용량 감소와는 구분한다. 압축 원본 백업은 Git/배포 대상에서 제외한다.

## 이후 작업

1. `npm run assets:optimize`: 파일을 변경하지 않고 변환 절감량 계산.
2. `node scripts/optimize-exercise-assets.mjs --write`: 원본 압축 보관 후 픽셀이 동일한 파일만 교체.
3. `npm run assets:check`: 참조·해시·출처·리디렉션·용량 한도 점검. build에서도 자동 실행.
4. 브랜치에서 검증 후 push. main 반영은 별도로 진행.

예산은 `data/asset-budget.json`에 이미지 전체 42 MiB, 단일 이미지 4 MiB로 설정했다. 의도적으로 운동 라이브러리를 확장할 때만 근거를 확인하고 조정한다. OCR 런타임 등 다른 자산의 용량은 이 예산에 포함되지 않는다.

## 검증

- 운동 라이브러리 테스트 3개 통과, production build 통과.
- 모든 구주소 93개를 로컬 Next 서버에서 308/목적지 일치 검증.
- 대표 대형 WebP의 크기 2464×1728 유지, Next Image 최적화 384px 요청 200 응답 확인.
