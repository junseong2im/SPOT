# SPOT 루틴 전용 해석기

## 구현 범위
- 글 붙여넣기: 요일/Day 별 분리, 운동명 줄임말, 4×10, 3세트 8~12회, 역순 12회씩 3세트, 표의 종목/세트/횟수 열.
- PNG/JPEG/WebP 사진을 브라우저 Tesseract.js 한국어+영어 OCR로 읽고, 인식된 글 검토 후 같은 가져오기 흐름으로 연결한다. PDF·영상·손글씨 정확도는 보장하지 않는다.
- 선택한 루틴의 세트/횟수 변경, 증감, 종목 제외/교체를 자연어로 제안한다. 편집기에 적용한 뒤 기존 저장 흐름을 따른다.
- 새 공통 루틴 여러 개를 하나의 트랜잭션으로 저장한다. 현재 크루의 멤버 권한, 리비전, 개수·숫자·ID를 서버에서 검사한다. 기존 루틴을 덮어쓰지 않는다.

## 로컬 AI 구성
- 1~3글자 해시 특징 1,024개와 5개 의도 클래스를 사용하는 소프트맥스 선형 분류기.
- `python scripts/train-routine-intent.py`로 외부 패키지·GPU 없이 재현 가능하다.
- 개발자가 작성한 합성 문장으로 학습한다. 중복 문장 제거 후 템플릿 및 운동명을 분리한 검증셋을 사용한다. 검증 데이터를 학습 업데이트에 사용하지 않는다.
- 학습/검증 자료와 오류: `data/routine-assistant/{train,heldout,evaluation}.json`.
- 이것은 범용 생성형 LLM이나 개인 맞춤 훈련 처방 모델이 아니다. 문장 분류 결과와 규칙이 충돌하면 수정하지 않고 구체적인 입력을 요청한다.
- 숫자 및 대상 운동은 규칙으로 별도 검사한다. 이름을 모르면 원문을 유지하고 사용자에게 확인을 요청한다. 탑세트·백오프처럼 복합 세트는 합산을 추측하지 않는다.
- 합성 문장 점수는 실제 사용자 정확도나 운동 안전성 검증 결과가 아니다. 실제 동의받은 사용자 자료로 평가하기 전에는 일반화 성능을 주장하지 않는다.

## 배포와 데이터
- 텍스트 해석·모델 추론·사진 OCR는 클라이언트에서 수행된다. 외부 유료 AI API 또는 로컬 PC의 상시 서버에 의존하지 않는다.
- 모델과 OCR 정적 자산은 Vercel에서 제공한다. 사진은 외부로 업로드하지 않는다. 사용자가 저장을 누르면 검토한 루틴과 원문 메모가 기존 Supabase 저장 경로로 전달된다.
- `npm run build`에서 `scripts/prepare-ocr.mjs`가 고정된 npm 패키지의 OCR 파일을 `public/ocr`에 복사한다. 첫 사진 인식 시 언어/엔진 다운로드가 필요하다. 일반 대시보드 진입에는 OCR 모델을 로드하지 않는다.
- 한 번에 원문 12,000자, 10개 루틴, 루틴당 30개 운동. 원문은 각 루틴 메모에 보존한다. 인식하지 못한 줄을 숨기지 않는다.
- OCR: tesseract.js 7.0.0 (Apache-2.0), tesseract.js-core (Apache-2.0), @tesseract.js-data/kor 및 eng 1.0.0 (package metadata MIT, upstream naptha/tessdata). 패키지 고정·lockfile과 자산 manifest SHA-256을 사용한다.
- 자동 학습·사용자 데이터 수집·운동 조건만으로 새 프로그램 생성은 아직 포함하지 않는다.

## 검증
- 텍스트·숫자 누락·범위·알 수 없는 운동·복합세트·자연어 변경·타 크루 접근·일괄 저장 롤백·중복 저장 방지 테스트.
- 합성 한국어 루틴 이미지의 실제 OCR 실행과 브라우저에서 가져오기/확정 흐름 확인. 실제 휴대폰·사진 다양성·손글씨는 별도 검증이 필요하다.
